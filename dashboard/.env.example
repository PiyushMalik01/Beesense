# BeeSense Dashboard Configuration
# Copy this to .env.local and fill in your values

# Cloudflare Worker API endpoint (no trailing slash)
NEXT_PUBLIC_API_URL=https://beesense-api.YOUR_SUBDOMAIN.workers.dev

# Device ID to monitor (must match DEVICE_ID in firmware)
NEXT_PUBLIC_DEVICE_ID=beesense-01
